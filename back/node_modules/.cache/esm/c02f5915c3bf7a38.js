let bcrypt;_c3b‍.x([["hashPassword",()=>hashPassword],["comparePassword",()=>comparePassword]]);_c3b‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]); //importing bcrypt

//password hashing

       const hashPassword = (password) =>{
    return new Promise((resolve, reject) =>{
        bcrypt.genSalt(12,(err,salt)=>{
            if(err){
                console.log('salting error');
                reject(err);
            }
            bcrypt.hash(password, salt, (err,hash)=>{
                if(err){
                    console.log('password hashing error');
                    reject(err);
                }
                resolve(hash);
            })
        })
    })
}
//it compares and returns true or false
       const comparePassword=(password, hashedPassword)=>{
    return bcrypt.compare(password,hashedPassword);
}